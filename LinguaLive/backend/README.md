# LinguaLive Backend

## Setup

1. `cd backend/app`
2. `pip install -r requirements.txt`
3. `uvicorn main:app --reload`

## Endpoints

- `/auth/login`, `/auth/register`
- `/translate/`
- `/tts/`
- `/stt/`